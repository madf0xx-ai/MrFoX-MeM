fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# Small, keeps the tool lightweight: vectorized retrieval scan (one matmul).
numpy>=1.24

# For real semantic embeddings + cross-encoder reranker, install the extra
# (heavier: pulls onnxruntime + a ~130 MB model on first use). Without it the
# tool still runs on a dependency-free hashing embedder and /health flags
# "degraded" so you know retrieval is keyword-level:
#     pip install "mrfox-mem[fastembed]"

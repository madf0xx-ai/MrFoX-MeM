# Dev / test dependencies (not needed to run MrFoX-MeM, only to test it).
-r requirements.txt
pytest>=8.0
httpx>=0.27          # enables fastapi.testclient (TestClient) in tests/test_api.py
numpy>=1.24          # optional runtime accelerator; here so CI covers the fast path
ruff>=0.6            # lint gate (matches the CI lint job)
pytest-cov>=5.0      # coverage reporting
